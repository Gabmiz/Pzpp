<Controls:MetroWindow x:Class="Pzpp.ComputersTable"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pzpp"
        mc:Ignorable="d"
        Title="Computers Table" Height="300" Width="500">
    <Grid>
                 <ListView  Grid.Row="1" ItemsSource="{Binding }" BorderBrush="DeepSkyBlue" Grid.ColumnSpan="3">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="COMPUTER ID" Width="110" DisplayMemberBinding="{Binding PC_ID}"/>
                            <GridViewColumn Header="IP" Width="200" DisplayMemberBinding="{Binding IP}"/>
                            <GridViewColumn Header="NAME" Width="200" DisplayMemberBinding="{Binding Name}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
</Controls:MetroWindow>
